# Blokir akses HTTP ke kredensial Google Service Account (Apache).
<Files "google_service_account.json">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order deny,allow
        Deny from all
    </IfModule>
</Files>
